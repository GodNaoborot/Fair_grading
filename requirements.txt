# Сэмплирование и вывод.
# Код совместим с двумя ветками: pymc 5.x / arviz 0.x и pymc 6.x / arviz 1.x.
# Различия в API arviz (hdi, compare, groups, поля результата loo) скрыты
# в src/az_compat.py, оба варианта проверены полным прогоном тестов.
pymc>=5.20
arviz>=0.18
pytensor>=2.20

# NUTS через numba/Rust. Родной бэкенд PyTensor без C++-компилятора
# разворачивается в чистый Python, и сэмплирование становится неприменимо
# медленным (см. раздел «Выбор сэмплера» в README).
nutpie>=0.13

# Численные библиотеки.
# Верхняя граница numpy обязательна: numba (приходит с nutpie и pytensor)
# требует numpy<2.5, и при более новой версии сэмплирование не запускается.
numpy>=2.0,<2.5
pandas>=2.0
scipy>=1.13

# Графики
matplotlib>=3.8

# Чтение и запись трасс в netCDF. arviz объявляет их не всегда, а без них
# az.from_netcdf падает с «their dependencies may not be installed»:
# h5netcdf — бэкенд для xarray, h5py — его собственная зависимость.
h5netcdf>=1.0
h5py>=3.0

# Ноутбуки
jupyter
ipykernel

# Тесты
pytest>=8.0

# Парсер исходной выгрузки (src/parser_lib.py)
openpyxl
xlrd>=2.0
